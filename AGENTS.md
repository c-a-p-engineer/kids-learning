# AGENTS.md

このリポジトリは、子ども向けブラウザ学習コンテンツを継続的に追加・改善するためのものです。AIエージェントは、既存機能、子どもの操作性、保護者の管理性、GitHub Pagesでの配布を同時に維持してください。

## 1. 最優先事項

1. 子どもが迷わず操作できること
2. スマートフォンとタブレットで表示・操作が破綻しないこと
3. 既存の学習記録を壊さないこと
4. 変更内容と仕様書を一致させること
5. 依存追加と大規模リファクタを避けること

## 2. リポジトリ構成

- `src/index.html`: 共通画面構造
- `src/main.ts`: ポータル、ルーティング、共通イベント
- `src/app/`: 共通機能、状態管理、描画、音声
- `src/contents/`: コンテンツ定義とゲーム実装
- `src/styles/`: 共通スタイル、コンテンツ別スタイル、端末対応
- `src/public/404.html`: GitHub Pagesの深いURL向けフォールバック
- `docs/`: 仕様、設計方針、会話・意思決定記録
- `scripts/build.sh`: Viteビルドと配布物整形
- `public/`: 自動生成される配布物。直接編集禁止

## 3. 絶対ルール

1. `public/` は直接編集しない。必ず `src/` を変更してビルドする。
2. GitHub Pagesで動くよう、アプリ内のパスとアセット参照は相対パスを基本とする。
3. 新しいnpm依存は、既存手段では実現できない場合に限る。
4. `localStorage` の既存キー、保存形式、意味を無断で変更しない。
5. 保存形式を変更する場合は、後方互換または明示的な移行処理を用意する。
6. UI・導線・保存仕様を変更したら、同一変更内で対応する `docs/` を更新する。
7. 子ども向け画面に、削除・初期化などの破壊的操作を露出させない。
8. 個人情報、認証情報、家庭内だけで使う情報をリポジトリへ記録しない。

## 4. 子ども向けUI基準

詳細は `docs/design/子ども向けUI方針.md` を正とします。

最低条件:

- 主要なタップ領域は原則44px以上
- 本文は原則16px以上。補助文も読めるコントラストを確保
- 色だけで状態を伝えず、文字・形・アイコンを併用
- 横スクロールを主要導線にしない
- スマホ縦幅360px程度でも主要操作が可能
- タブレット幅768px以上では余白を増やすだけでなく、カードを複数列化して視線移動を短縮
- ブラウザの文字拡大とピンチズームを妨げない
- `:focus-visible` を消さない
- `prefers-reduced-motion` に追従する
- アニメーションは学習結果の理解を助ける範囲に限定
- 誤操作時に即座にデータを失わない

## 5. コンテンツ追加手順

1. `src/contents/<content-id>.ts` にタイトル、説明、タグを定義する
2. 必要な実装を `src/contents/` に追加する
3. `src/contents/index.ts` に登録する
4. 既存のルーティング規則 `/<contentId>/<view>` に従う
5. `docs/<コンテンツ名>.md` を `docs/仕様書テンプレート.md` に沿って作成する
6. `docs/コンテンツ一覧.md` と `README.md` の一覧を更新する
7. スマホ、タブレット、キーボードで確認する

## 6. 仕様書運用

- 1コンテンツにつき1仕様書を基本とする
- トップ画面の変更は `docs/トップ画面.md` に反映する
- 共通UIの判断は `docs/design/` に記録する
- 技術的な構造変更は必要に応じて `docs/architecture.md` に記録する
- 仕様書と実装が矛盾する場合、同一作業内で解消する
- 未実装案を確定仕様として書かない。未確定事項として明示する

## 7. 会話・意思決定記録

ユーザーとの会話で、要件、採用案、却下案、運用ルール、重要な前提が決まった場合は `docs/conversations/` に要約を追加します。

記録する項目:

- 日付
- 背景と目的
- ユーザー要求の要約
- 決定事項
- 実施した変更
- 保留事項
- 関連ファイル、Issue、PR

記録しないもの:

- 会話全文の機械的な転記
- 個人情報や子どもの識別情報
- 認証情報、秘密情報、ローカル環境固有の値
- 雑談や実装判断に影響しない発言

同日に同じ目的の会話が続く場合は、既存ファイルへ追記します。運用詳細は `docs/conversations/README.md` を参照してください。

## 8. 実装方針

- 差分を小さく保つ
- 既存の型と命名規則に合わせる
- 共通化は重複が実害になった時点で行う
- マジックナンバーは意味のある定数へ切り出す
- 外部通信は失敗しても学習画面全体を止めない
- DOMへ外部文字列を入れる場合はサニタイズする
- タイマーやアニメーションは画面離脱時に停止する
- イベントリスナーの重複登録を避ける
- コンテンツ固有の保存キーは衝突しない名前にする

## 9. 必須確認

変更後は次を実行します。

```bash
npm ci
npm run typecheck
./scripts/build.sh
```

UI変更時の確認項目:

- [ ] 360 x 640前後のスマホ縦
- [ ] 768 x 1024前後のタブレット縦
- [ ] タブレット横またはPC
- [ ] 文字を200%程度に拡大して主要操作が残る
- [ ] Tabキーで主要ボタンへ移動できる
- [ ] 画面回転後に操作不能にならない
- [ ] 既存の学習記録が読み込める
- [ ] 直接URLから対象コンテンツを開ける

## 10. PR本文

```markdown
## Summary
- 変更内容
- 変更理由

## Child UX / Parent UX
- 子ども向け画面への影響
- 保護者向け画面への影響

## Documentation
- 更新した仕様書・会話記録

## Verification
- [ ] npm run typecheck
- [ ] ./scripts/build.sh
- [ ] スマホ縦
- [ ] タブレット縦
- [ ] キーボード操作

## Risks
- 保存データへの影響
- 既知の制限
```

## 11. 禁止事項

- `public/` の直接編集
- データ削除確認の省略
- 既存保存キーの無断変更
- 子ども向け画面への設定項目の過剰追加
- 外部CDN、フォント、分析タグの無断追加
- UI修正と無関係な全面リファクタ
- 仕様書を更新せずに画面・導線を変更すること
- 会話ログへ個人情報や秘密情報を残すこと

## 12. 判断に迷った場合

1. 既存機能を維持する
2. 子どもの主要操作を優先する
3. スマホ縦で成立する最小変更を選ぶ
4. 保護者向け機能は子ども向け導線から分離する
5. 未確定事項を勝手に仕様化せず、文書に保留として残す
