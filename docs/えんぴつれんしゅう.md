# えんぴつれんしゅう 仕様書

## 1. 概要

- コンテンツ名: えんぴつれんしゅう
- contentId: `pencil-practice`
- 公開URL: `/pencil-practice`
- 学習経路: STEP 7
- 目的: 文字を書く前に、始点から終点へ線を運ぶ動作、曲線、角、閉じた図形を練習する
- 対象: 指やタッチペンで線を引ける幼児〜小学生

## 2. 学習上の位置付け

時計を読む課題の後、かきとりマスターの前に配置します。

```text
時計を読む
  ↓
線を運ぶ
  ↓
文字を書く
```

鉛筆の持ち方、姿勢、筆圧そのものは端末だけでは判定しません。本コンテンツは運筆経路の練習に限定します。

## 3. 終了条件

- 1セットは5問
- 5問完了後に必ず結果画面へ移動する
- 自動で次のセットを開始しない
- 再挑戦は結果画面からメニューへ戻り、明示的に開始する
- 時間制限は設けず、問題数によって終了させる

これは [学習セッション方針](design/学習セッション方針.md) の「問題数が確定している課題」に該当します。

## 4. 難易度

### やさしい

- 横線
- 縦線
- 斜め線
- 大きく緩やかな波線
- 大きな山形
- 判定用の道幅を広くする

### ふつう

- 波線
- ジグザグ
- S字
- 円
- 階段状の線

### むずかしい

- 三角形
- 四角形
- ひし形
- 渦巻き
- 細かな波線
- 判定用の道幅を狭くする

## 5. 操作フロー

1. 難易度を選ぶ
2. 始点 `①` から終点 `🏁` まで線を引く
3. 必要なら `🧹 やりなおす` で現在の線を消す
4. `💮 できた！` を押す
5. 参考スコアを表示して次の問題へ進む
6. 5問終了後に結果を表示する
7. `▶️ どうがを みる` で5問分の描画過程を確認できる
8. `🎮 メニューへ` で開始画面へ戻る

線がほとんど描かれていない場合は次へ進めず、短い案内を表示します。

## 6. 判定

判定は次の3要素を用いた参考値です。

- お手本の経路をどの程度通ったか
- 始点付近から開始したか
- 終点付近まで到達したか

難易度ごとに判定用の許容幅を変えます。

この点数は医学的・療育的な評価ではありません。低い点数でも課題は完了扱いとして次へ進み、子どもを責める表現を使用しません。

## 7. 描画仕様

- HTML Canvasを使用する
- マウス、指、タッチペンをPointer Eventsで統一して扱う
- 描画領域ではブラウザのスクロールより線入力を優先する
- お手本は太い通過範囲と中央の破線で表示する
- 利用者が描いた線はテーマの主要色で表示する
- キャンバス内部座標は正規化し、画面サイズに依存しない

## 8. 動画確認

- 実際の画面録画データを常時保存せず、描画座標と経過時間から再生する
- 5問分を問題順に連続再生する
- 結果画面から直近のセットを再生できる
- 履歴画面から保存済みの各セットを再生できる
- 再生画面には問題名、現在の問題数、始点、終点を表示する
- `🔁 もういちど` で再度再生できる
- MediaRecorderとCanvas動画出力に対応するブラウザではWebM形式で保存できる
- 動画保存に非対応のブラウザでも画面内再生は利用できる
- 既存履歴など描画データがない履歴には `どうがなし` と表示する

書き取りと同様に、動画ファイルそのものではなく座標データを正として、必要時に再生・動画生成します。

## 9. 記録

### 点数履歴

- 保存キー: `pencil_practice_v1_history`
- 保存件数: 直近50件
- 保存項目:
  - 実施日時
  - 難易度
  - 平均参考スコア
  - 基準以上だった問題数
  - 総問題数

### 動画再生データ

- 保存キー: `pencil_practice_v1_replays`
- 保存件数: 直近20件
- 保存項目:
  - 点数履歴のID
  - 問題名
  - 正規化した描画座標
  - 描画開始からの経過時間
  - ストロークの区切り
- 保存容量を抑えるため、近接した座標を間引く
- 点数履歴を削除した場合は、対応する動画再生データも削除する
- 保存容量が不足した場合は保持件数を縮小し、学習処理自体は継続する

従来の点数履歴形式は変更せず、動画再生データを別の保存キーへ追加します。

## 10. アクセシビリティ・UI

- 主要ボタンは44px以上のタップ領域を確保する
- 始点と終点は色だけでなく `①` と `🏁` で示す
- スマートフォン縦画面で描画領域と主要操作を確認できる
- タブレットでは描画領域を過度に巨大化しない
- 動画再生画面は画面幅に合わせて縮小し、横スクロールさせない
- 再生・保存・閉じる操作には絵文字と文字を併記する
- `prefers-reduced-motion` では不要なトランジションを停止する
- 強制カラーモードでもキャンバス境界と操作ボタンを判別できるようにする

## 11. 受け入れ条件

- [ ] ポータルのSTEP 7に表示される
- [ ] かきとりマスターがSTEP 8になる
- [ ] `/pencil-practice` を直接開ける
- [ ] 3難易度を選択できる
- [ ] 各セットが5問で終了する
- [ ] 始点・終点・お手本・利用者の線を表示できる
- [ ] 指、タッチペン、マウスで線を引ける
- [ ] `やりなおす` で現在の問題だけを消せる
- [ ] 5問終了後に平均参考スコアと結果を表示する
- [ ] 結果画面から5問分の描画を再生できる
- [ ] 履歴画面から動画再生とWebM保存を選べる
- [ ] 古い点数履歴は表示でき、動画データがない場合は `どうがなし` になる
- [ ] 点数履歴50件と動画再生データ20件を独立して保持する
- [ ] 履歴削除時に対応する動画データも削除される
- [ ] 結果画面から自動再開しない

## 12. 関連実装

- `src/pencil-practice.ts`
- `src/pencil-practice-video.ts`
- `src/pencil-practice-video-sync.ts`
- `src/styles/pencil-practice.scss`
- `src/styles/pencil-practice-video.scss`
- `src/contents/pencil-practice.ts`
- `src/contents/index.ts`
- `src/common-ui.ts`
